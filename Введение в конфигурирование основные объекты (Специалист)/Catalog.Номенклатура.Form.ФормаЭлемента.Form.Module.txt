
&НаСервереБезКонтекста
Функция ПолучитьОснЕдиницауслугНаСервере()
	 ОснЕдиница = Константы.ОсновнаяЕдиницаУслуг.Получить();
	 Возврат ОснЕдиница;
КонецФункции

&НаКлиенте
Процедура УслугаПриИзменении(Элемент)
	Если  Объект.Услуга Тогда 
		Объект.ОснЕдиницаИзмерения = ПолучитьОснЕдиницауслугНаСервере();
	Иначе
		 Объект.ОснЕдиницаИзмерения  = NULL;
	КонецЕсли;
	
КонецПроцедуры
